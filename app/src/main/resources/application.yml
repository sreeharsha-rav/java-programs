spring:
    application:
        name: MealMap
    r2dbc:
        url: r2dbc:postgresql://localhost:5432/mealmap
        username: postgres
        password: postgres

# spring doc
springdoc:
    api-docs:
        path: /v3/api-docs
    swagger-ui:
        path: /swagger-ui.html

# server port
server:
    port: 3000