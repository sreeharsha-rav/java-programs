spring:
    application:
        name: MealMap
    r2dbc:
        url: r2dbc:h2:mem:///mealmap
        username: sa
        password: password

# spring doc
springdoc:
    api-docs:
        path: /v3/api-docs
    swagger-ui:
        path: /swagger-ui.html

# server port
server:
    port: 8080